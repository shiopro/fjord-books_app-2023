<p style="color: green"><%= notice %></p>

<h1><%= I18n.t('headlines.books') %></h1>

<div id="books">
  <% @books.each do |book| %>
    <%= render book %>
    <p>
      <%= link_to I18n.t('buttons.show'), book %>
    </p>
  <% end %>
</div>

<%= link_to I18n.t('buttons.new'), new_book_path %>
